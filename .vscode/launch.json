{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "NodeMCU-Debug",
            "type": "nodemcu",
            "request": "launch",
            "program": "${file}"
        }
    ]
}